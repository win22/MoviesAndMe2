import React from 'react'
import { StyleSheet, Text } from 'react-native'

class Favorites extends React.Component {
    render(){
        return (
            <Text>Mes Favorites</Text>
        )
    }
}
 const style = StyleSheet.create({})
export default Favorites